<Page
    x:Class="TouchKeyboardAudio.Uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="DragRegion" Background="Transparent">
            <TextBlock
                Text="Touch Keyboard Audio"
                FontSize="12"
                Margin="12,0,0,0"
                VerticalAlignment="Center" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid MaxWidth="680" Margin="40,26,40,30" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock
                        Text="触摸键盘音量"
                        FontSize="32"
                        FontWeight="Light" />
                    <TextBlock
                        Text="Windows 10 触摸键盘按键反馈音"
                        FontSize="12"
                        Opacity="0.65"
                        Margin="1,5,0,0" />

                    <Grid Margin="0,28,0,0">
                        <TextBlock
                            x:Name="DbText"
                            Text="+20.0 dB"
                            FontSize="44"
                            FontWeight="Light"
                            Foreground="{ThemeResource SystemControlHighlightAccentBrush}" />
                        <TextBlock
                            x:Name="GainText"
                            Text="10.00×"
                            FontSize="14"
                            Opacity="0.7"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center" />
                    </Grid>

                    <Slider
                        x:Name="GainSlider"
                        Minimum="-20"
                        Maximum="30"
                        Value="20"
                        StepFrequency="0.5"
                        TickFrequency="0.5"
                        IsThumbToolTipEnabled="False"
                        ValueChanged="GainSlider_ValueChanged"
                        Margin="0,8,0,0" />

                    <Grid Margin="0,2,0,0">
                        <TextBlock Text="-20 dB" FontSize="11" Opacity="0.58" HorizontalAlignment="Left" />
                        <TextBlock Text="0 dB" FontSize="11" Opacity="0.58" HorizontalAlignment="Center" />
                        <TextBlock Text="+30 dB" FontSize="11" Opacity="0.58" HorizontalAlignment="Right" />
                    </Grid>

                    <Border
                        Margin="0,24,0,0"
                        Padding="0,15,0,0"
                        BorderThickness="0,1,0,0"
                        BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock
                                    x:Name="ModeText"
                                    Text="IEEE Float32 Direct Gain"
                                    FontSize="12"
                                    Foreground="{ThemeResource SystemControlHighlightAccentBrush}" />
                                <TextBlock
                                    x:Name="EstimateText"
                                    FontSize="12"
                                    Opacity="0.68"
                                    Margin="0,5,0,0" />
                                <TextBlock
                                    x:Name="StatusText"
                                    Text="尚未应用"
                                    FontSize="12"
                                    Opacity="0.68"
                                    Margin="0,5,0,0" />
                            </StackPanel>

                            <TextBlock
                                Grid.Column="1"
                                Text="5 个 WAV · Float32"
                                FontSize="11"
                                Opacity="0.58"
                                Margin="20,0,0,0"
                                VerticalAlignment="Bottom" />
                        </Grid>
                    </Border>

                    <Grid Margin="0,34,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ProgressRing
                                x:Name="BusyRing"
                                Width="18"
                                Height="18"
                                IsActive="False"
                                Visibility="Collapsed" />
                            <TextBlock
                                Text="UWP 前端 · 管理员后端"
                                FontSize="11"
                                Opacity="0.58"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center" />
                        </StackPanel>

                        <Button
                            x:Name="RestoreButton"
                            Grid.Column="1"
                            Content="恢复原版"
                            MinWidth="102"
                            Height="32"
                            Click="RestoreButton_Click" />

                        <Button
                            x:Name="ApplyButton"
                            Grid.Column="3"
                            Content="应用"
                            MinWidth="88"
                            Height="32"
                            Background="{ThemeResource SystemControlHighlightAccentBrush}"
                            Foreground="White"
                            Click="ApplyButton_Click" />
                    </Grid>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
